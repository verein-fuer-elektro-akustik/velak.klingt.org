name: build

on:
  workflow_dispatch:
    inputs:
      url:
        description: 'Site baseurl'
        default: 'https://velak.klingt.org'
        required: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          lfs: true
      - uses: actions/setup-node@v3
        with:
          node-version: 16
          cache: 'npm'
      - name: Cache resources
        id: cache-resources
        uses: actions/cache@v3
        with:
          path: resources
          key: hugo-resources
      - name: Setup npm
        run: npm install
      - name: Build style
        run: npm run prod:style
      - name: Build site
        run: npm run prod:hugo -- --baseURL ${{ inputs.url }}
      - name: Store
        uses: actions/upload-artifact@v3
        with:
          name: velak.klingt.org
          path: public

